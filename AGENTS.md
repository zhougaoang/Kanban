# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

> 本项目锁定 Expo SDK 54（App Store 版 Expo Go 停更，只支持 SDK 54）。
> 不要升级 SDK。查阅文档请用 v54.0.0，不要用 latest。
